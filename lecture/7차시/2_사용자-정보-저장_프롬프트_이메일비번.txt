이메일/비밀번호 회원가입이 성공하면
Firestore users 컬렉션에 사용자 기본 정보를 저장하도록 수정한다.

문서 ID는 Firebase Authentication의 user.uid를 사용한다.

저장 정보:
uid, displayName, email, photoURL, createdAt

Firestore 저장에는 setDoc을 사용하고,
Firestore 저장 실패 시 오류를 확인할 수 있도록 처리한다.
기존 회원가입 기능과 다른 컴포넌트는 불필요하게 수정하지 않는다.